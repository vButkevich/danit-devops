instance-nginx

startup script

subnet public
MEtadate version V2 only


user-data-script logged
sudo less /var/log/cloud-init-output.log



sudo less /var/lib/instance/user-data.txt


get internal private ip
run two comansd
google: aws imds token
<!-- # Step 1: Generate a session token (valid for 6 hours / 21,600 seconds)
TOKEN=$(curl -X PUT "http://169.254.169" -H "X-aws-ec2-metadata-token-ttl-seconds: 21600") -->

<!-- # Step 2: Use the token to fetch specific metadata (e.g., Instance ID)
curl -H "X-aws-ec2-metadata-token: $TOKEN" http://169.254.169 -->

https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/configuring-instance-metadata-service.html#instance-metadata-ex-1
TOKEN=`curl -X PUT "http://169.254.169.254/latest/api/token" -H "X-aws-ec2-metadata-token-ttl-seconds: 21600"` \
&& curl -H "X-aws-ec2-metadata-token: $TOKEN" http://169.254.169.254/latest/meta-data/    

curl -H "X-aws-ec2-metadata-token: $TOKEN" http://169.254.169.254/latest/meta-data/



chnge default nginx page to show local ip

/var/www/html$ vim index.nginx-debian.html

/etc/init.d/nginx restart


ping https://18.199.84.182/


create launch template

systemctl status nginx
sudo systemctl status nginx
sudo ss -tulpn | grep -E ':80|:443'


create auto Scaling Group