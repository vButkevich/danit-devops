APP_NAME="spring-petclinic"
APP_DIR="/home/vagrant/${APP_NAME}"
DB_NAME="petclinic"
DB_USERNAME="appuser"
DB_PASSWORD="12345678"
DB_HOST="192.168.99.10"
DB_PORT="5432"
DB_URL="jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}"
APP_JAR="${APP_DIR}/target/spring-petclinic-4.0.0-SNAPSHOT.jar"
# APP_PARAMETERS="--database=postgres --spring.datasource.url=${DB_URL} --spring.datasource.username=${DB_USERNAME} --spring.datasource.password=${DB_PASSWORD}"
# APP_EXECUTABLE="java -jar ${APP_JAR} ${APP_PARAMETERS}"
# APP_EXPORT="export SPRING_PROFILES_ACTIVE=postgres; export POSTGRES_URL=${DB_URL}; export POSTGRES_USER=${DB_USERNAME}; export POSTGRES_PASS=${DB_PASSWORD};"
# APP_EXECUTABLE="${APP_EXPORT} java -jar ${APP_JAR}"
APP_EXECUTABLE="java -jar ${APP_JAR}"